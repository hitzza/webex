/* eslint-disable no-fallthrough */
/* eslint-disable no-case-declarations */
let item = 1;

switch (item) {
  case 1:
    let sport;
    break;
  case 2:
  // let sport;
  default:
    console.log(sport);
}
//case, default는 블록 스코프가 아니기 때문에 에러 x
